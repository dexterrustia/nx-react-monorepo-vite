import Icon, { IconOptions } from '../icon';
import svg from './year-wheel-hr-icon.svg';

export const YearWheelHrIcon = (props: IconOptions): JSX.Element => (
  <Icon svg={svg} alt="year wheel hr icon" options={props} />
);

export default YearWheelHrIcon;
