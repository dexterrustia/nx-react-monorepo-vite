import Icon, { IconOptions } from '../icon';
import svg from './Min bht ikon.svg';

export const BhtAppSwitchIcon = (props: IconOptions): JSX.Element => (
  <Icon svg={svg} alt="BHT app switch icon" options={props} />
);

export default BhtAppSwitchIcon;
